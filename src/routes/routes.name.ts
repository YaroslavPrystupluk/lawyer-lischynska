export enum COMMON_ROUTES {
  HOME = "/",
  PRICING = "pricing",
  ABOUT = "about",
  SERVICES = "services",
  CONTACT = "contact",
  BLOG = "blog",
  BLOG_POST = "blog/:id",
  NOT_FOUND = "404",
}

export enum ADMIN_ROUTES {
  LOGIN = "/login",
}
